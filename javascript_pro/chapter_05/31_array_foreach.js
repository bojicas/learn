// Array Type - iterative methods forEach()

var numbers = [1, 2, 3, 4, 5, 4, 3, 2, 1];

numbers.forEach(function (item, index, array) {
  console.log(item + ' at index ' + index + ' in array ' + array);
});
