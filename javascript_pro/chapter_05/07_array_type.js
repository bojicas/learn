// Array Type - literal notation

var colors = ["red", "blue", "green"];
var names = [];
var values = [1, 2, ]; // avoid
var options = [,,,,,]; // avoid

console.log(colors);
console.log(names);
console.log(values);
console.log(options);